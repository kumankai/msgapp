import { View, Text } from "react-native";

export default function SignupPage({ navigation }) {
    return (
        <View>
            <Text>
                Sign Up
            </Text>
        </View>
    )
}